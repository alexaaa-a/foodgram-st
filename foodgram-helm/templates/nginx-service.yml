apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80
  selector:
    app: nginx