apiVersion: v1
kind: ConfigMap
metadata:
  name: db-configmap
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "db.labels" . | nindent 4 }}
data:
  host: {{ .Values.primary.service.name }}
  port: "{{ .Values.env.DB_PORT }}"